<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<!-- DEV -->
<properties>
    <comment>Properties for a local system</comment>

    <!-- Email properties -->
    <entry key="mail-from-name">clarin.dk svar-ikke</entry>
    <entry key="mail-port">25</entry>
    <entry key="mail-server">mailgate.sc.ku.dk</entry>
    <entry key="mail-from">no-reply@127.0.0.0</entry>
    <entry key="adm-email">bart@cst.dk</entry>

    <!-- eSciDoc core properties -->
    <entry key="repoServiceUrl">http://127.0.0.0/ir/item/</entry>
    <entry key="admin-user-handle">secret</entry>

    <!-- Host names -->
    <entry key="core-server">http://127.0.0.0</entry>
    <entry key="www-server">http://localhost:8080</entry>

    <!-- Host name of infrastructure as made known to integrated tools (to fetch data from or send data to) -->
    <entry key="baseUrlTools">http://127.0.0.0</entry>
    <!-- Host name used to access resources (in combination with serviceUrl and item id) -->
    <!-- Location of staging area 
           remember to edit /etc/httpd/conf.d/proxy_ajp.conf:
             ProxyPass /toolsdata !
        -->
    <entry key="documentRoot">C:/projects/clarin/trunk/dkclarin-services</entry>
    <!-- The value of the stagingArea key must also be the name of a service,
           so that all fetches of results by users are under control of the service! -->
    <entry key="stagingArea">/tools/work/data/</entry>
    <entry key="toolsHome">C:/projects/clarin/trunk/dkclarin-services/tools/work/</entry>
    <entry key="toolsProg">toolsProg.bra</entry>
    <entry key="tempdir">work/Catalina/localhost/tools</entry>
    <!-- used for testing and hot reloading of toolsProg.bra -->
    <entry key="password"></entry>

    <!--  86400000 == one day  -->
    <!-- 172800000 == two days -->
    <entry key="deleteAfterMillisec">172800000</entry>

    <!-- The only server that is allowed to start workflows (the web-server) -->
    <entry key="accepted-workflow-starter">192.38.29.153</entry>
</properties>


